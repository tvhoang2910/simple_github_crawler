# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=github_crawler
DB_USER=postgres
DB_PASS=your_postgres_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# GitHub Tokens (required - separated by commas)
# ⚠️ NEVER commit actual tokens to Git!
# Replace with your token when running locally
GITHUB_TOKENS=your_github_token_here

# Optional: Batch size
BATCH_SIZE=100
